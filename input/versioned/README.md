# Versioned Input Files

This folder contains external documentation files deliberately approved for
Git and assistant access. Files promoted into `docs/` or `assets/` become
maintained documentation content. Git versioning does not authorize publication.
